const Loading = () => {
  return (
    <div className='flex flex-col min-h-[calc(100vh-150px)] justify-center items-center'>
      <span className='loading loading-spinner loading-lg'></span>
    </div>
  );
};

export default Loading;
